export const USDFormatter = Intl.NumberFormat('en-US', {
  style: 'currency',
  currency: 'USD',
})
export const BRLFormatter = Intl.NumberFormat('pt-BR', {
  style: 'currency',
  currency: 'BRL',
})
export const EURFormatter = Intl.NumberFormat('de-DE', {
  style: 'currency',
  currency: 'EUR',
})
